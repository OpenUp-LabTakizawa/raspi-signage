name: 🧪 Test

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  pull_request:
  push:
    branches: main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    outputs:
      is_success: ${{ steps.test.outputs.is_success }}
      test_log: ${{ steps.test.outputs.test_log }}

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: 🐣 Setup mise
        uses: jdx/mise-action@v4.3.0
        with:
          install_args: bun

      - name: 📦 Install dependencies
        run: bun i

      - name: 🧪 Test app
        id: test
        # Unit tests fully mock the database, storage, and auth layers, so no
        # external services are needed. DATABASE_URL and the Better Auth pair
        # only have to exist, so that module initialization in
        # `src/auth/server.ts` does not throw at import time (the actual auth
        # instance is mocked in tests); `mise-action` exports them from
        # `mise.toml`'s `[env]`.
        env:
          FORCE_COLOR: 0
        run: |
          STATUS=0
          bun test:unit > test.log 2>&1 || STATUS=$?
          cat test.log
          if [ "$STATUS" -ne 0 ]; then
            echo "is_success=false" >> "$GITHUB_OUTPUT"
          else
            echo "is_success=true" >> "$GITHUB_OUTPUT"
          fi
          UUID=$(uuidgen)
          {
            echo "test_log<<EOF_$UUID"
            awk '
              /^::group::/ { sub(/^::group::/, ""); block = $0 "\n"; grouped = 1; failed = 0; next }
              /^::endgroup::/ { if (failed) printf "%s", block; grouped = 0; next }
              /^::error/ { next }
              grouped { if ($0 !~ /^\(pass\)/) block = block $0 "\n"; if ($0 ~ /^\(fail\)/) failed = 1; next }
              !/^\(pass\)/ { print }
            ' test.log | cat -s
            echo "EOF_$UUID"
          } >> "$GITHUB_OUTPUT"

      - name: 💔 Fail if test failed
        if: steps.test.outputs.is_success == 'false'
        run: exit 1

  upload:
    runs-on: ubuntu-latest
    needs: test
    if: (success() || failure()) && github.event_name == 'pull_request'

    steps:
      - name: 📝 Create comment
        env:
          ACTOR: ${{ github.actor }}
          IS_SUCCESS: ${{ needs.test.outputs.is_success }}
          REPO: ${{ github.repository }}
          RUN_ID: ${{ github.run_id }}
          SHA: ${{ github.event.pull_request.head.sha }}
          TEST_LOG: ${{ needs.test.outputs.test_log }}
          WORKFLOW_NAME: ${{ github.workflow }}
        run: |
          UUID=$(uuidgen)
          if [ "$IS_SUCCESS" = "true" ]; then
            cat <<EOF_$UUID > comment.txt
          # ✅ Test Passed
          Failed tests were fixed.
          <!-- generated-comment [$WORKFLOW_NAME](https://github.com/$REPO/.github/workflows/test.yml) -->
          EOF_$UUID
          else
            cat <<EOF_$UUID > comment.txt
          # ❌ Test Failed
          @$ACTOR, your commit $SHA failed the test.

          <details>
          <summary><strong>Test Log</strong></summary>

          \`\`\`shell
          $TEST_LOG
          \`\`\`

          </details>

          👀[View in Actions](https://github.com/$REPO/actions/runs/$RUN_ID)
          <!-- generated-comment [$WORKFLOW_NAME](https://github.com/$REPO/.github/workflows/test.yml) -->
          EOF_$UUID
          fi

      - name: 🆙 Upload test comment
        uses: actions/upload-artifact@v7
        with:
          name: comment
          path: comment.txt
